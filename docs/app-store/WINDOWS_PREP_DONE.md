# Windows Preparation Completed

This file records what has been prepared before moving to macOS.

## Completed On Windows

- Current PWA v21 preserved under `legacy-pwa/`.
- Current PWA zip backup generated under `backups/`.
- New frontend scaffold created under `frontend/`.
- Capacitor config prepared for iOS under `frontend/capacitor.config.ts`.
- Supabase Edge Function scaffold created under `supabase/functions/app-api`.
- Shared Edge Function auth/CORS/client helpers created.
- Industrial database/RLS migration created under `supabase/migrations/`.
- Environment template created at `.env.example`.
- App Store/macOS handoff documentation created under `docs/app-store/`.

## Still Requires macOS

- Installing iOS platform with Capacitor.
- Opening Xcode project.
- Signing and provisioning.
- Creating archive.
- Uploading to App Store Connect.

## Still Requires Supabase Dashboard/CLI

- Apply migration to a staging project.
- Create Edge Function secrets.
- Deploy Edge Function.
- Create Supabase Auth users and profiles.
- Test RLS.
